version: "2"

linters:
  default: none
  enable:
    - govet
    - misspell
    - unconvert
    - whitespace

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

run:
  timeout: 5m
  modules-download-mode: readonly
